# CMakeLists.txt - Root
# MoodLink v3.0 - ESP-IDF + esp-tflite-micro

cmake_minimum_required(VERSION 3.16)

# Include ESP-IDF CMake project
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Project name
project(moodlink)
